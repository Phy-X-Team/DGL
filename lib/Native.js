export var Number = self.Number;

export var Object = self.Object;

export var String = self.String;

export var Array = self.Array;

export var Boolean = self.Boolean;

export var Uint8Array = self.Uint8Array;

export var Uint16Array = self.Uint16Array;

export var Uint32Array = self.Uint32Array;

export var Float32Array = self.Float32Array;

export var TextEncoder = self.TextEncoder;

export var TextDecoder = self.TextDecoder;

export var Proxy = self.Proxy;
